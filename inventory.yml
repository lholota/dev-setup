---
all:
  children:
    wsl:
      hosts:
        # The localhost is registered as two different "hosts" because each needs a different type of connection
        localhost-wsl
    win:
      hosts:
        localhost-win
  vars:
    public_gpg_key_url: https://keybase.io/lholota/pgp_keys.asc